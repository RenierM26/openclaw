FROM ghcr.io/openclaw/openclaw:latest

USER root
RUN apt-get update && \
    apt-get install -y --no-install-recommends \
      docker.io \
      git \
      curl \
      jq \
      ffmpeg \
      ripgrep \
      build-essential \
    && rm -rf /var/lib/apt/lists/*

USER node